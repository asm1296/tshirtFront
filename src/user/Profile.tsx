import React from 'react';
import Base from '../core/Base';

const Profile = ()=>{
    return(
        <Base title="Profile Page" description="Keep updated your profile just like we keep our product list">
            <h3>This is Profile Page</h3>
        </Base>
    )
}

export default Profile;